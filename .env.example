# Admin CMS access token (used to log into /admin and authorize pricing/lead APIs).
# Defaults to "veer-admin" if unset — CHANGE THIS IN PRODUCTION.
ADMIN_TOKEN=veer-admin

# Public site URL (used for canonical URLs, sitemap, OpenGraph).
NEXT_PUBLIC_SITE_URL=https://veeraluminium.in
